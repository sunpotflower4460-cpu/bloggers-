# DESIGN_INVENTORY.md

デザイン主権が `demo` の場合にのみ使う。既存デモからトークンを抽出するシート。

## 運用ルール

1. デザイン主権が `demo` のとき、`tokens.css` はデモから抽出して作る。`DESIGN_BRIEF.md` の導出は行わない
2. 抽出後のリファクタは抽出トークン経由のみ。デモの顔を保ったまま、ハードコード禁止の規律だけが効く
3. `craft/` の材料は、デモに存在しない画面にのみ適用する（空状態・エラー・設定・読み込み中）。適用時も必ず抽出トークンで再スキンする
4. デモに既に存在する表現は、修正対象ではなく `INVENTORY.md` での確認対象

### T-001 アクセント色
- デモ内の値: #xxxxxx（src/App.css:42 ほか6箇所）
- 私の解釈: 主要アクションと選択状態に使われている
- 確認: この認識で合っていますか？
- [ ] 合っている  [ ] 違う →（　　）
- 割り当て先トークン: --accent
